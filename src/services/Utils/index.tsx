import { getAuthTokenId } from './getAuthToken'
import { getMembers, getGroupName, getRoleName } from './users.service'

export const UtilService = {
  getAuthTokenId,
  getMembers,
  getGroupName,
  getRoleName,
}
